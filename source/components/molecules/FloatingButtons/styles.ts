import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  box: {
    position: 'absolute',
    marginHorizontal: 16,
    marginVertical: 8,
  },

  button: {
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 10,
  },
})
